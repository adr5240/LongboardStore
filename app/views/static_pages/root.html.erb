<script type="text/javascript">
    <% if is_logged_in? %>
        window.currentUser = <%= render(
            "api/users/user.json.jbuilder",
            user: current_user
        ).html_safe %>
    <% end %>

    <% current_order %>
</script>

<main id="root"></main>
